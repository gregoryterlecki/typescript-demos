var age = 23;
age = '23'; // <- we get an error here because we're trying to assign a string to a variable with type "number".
var firstname = 'Chrisopher';
firstname = 'Christine'; // <- no error here; we're assigning a new string value to a variable that was already a string.
