var pow = function (a, b) {
    return Math.pow(a, b);
};
var powAsString = function (a, b) {
    return Math.pow(a, b).toString();
};
console.log('pow', pow(2, 3));
console.log('powAsString', powAsString(2, 3));
